version: "2"
linters:
  enable:
  - gosec
  - misspell
formatters:
  enable:
    - gofmt
issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - gosec
        - govet
    - path: example/
      linters:
        - gosec
